# run: conda env create --file environment.yml
name: plantcv
dependencies:
  - python=3.7
  - matplotlib>=1.5
  - numpy>=1.11
  - pandas
  - python-dateutil
  - scipy
  - scikit-image
  - plotnine
  - nb_conda
  - opencv<4, >=3.4
channels:
  - defaults
  - conda-forge
