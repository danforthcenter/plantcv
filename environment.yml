# run: conda env create --file environment.yml
name: test-environment
dependencies:
  - python=3.6
  - matplotlib>=1.5
  - numpy>=1.11
  - pandas
  - python-dateutil
  - scipy
  - scikit-image
  - pytest
  - plotnine
  - coveralls
  - pip:
    - opencv-python
channels:
  - conda-forge
