# run: conda env create --file environment37.yml
name: test-environment
dependencies:
  - python=3.7
  - matplotlib>=1.5
  - numpy>=1.11, <1.17
  - pandas
  - python-dateutil
  - scipy < 1.3
  - scikit-image==0.14.2
  - pytest
  - plotnine
  - coveralls
  - nb_conda
  - pip
  - pip:
    - opencv-python<4, >=3.4
channels:
  - defaults
  - conda-forge
